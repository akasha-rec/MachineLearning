240509

파이프라인 : 데이터 분석 > 전처리(ex. NA 처리) //pandas
	> 전처리(ex. ~encoding) > 모델 > 교차검증(+ 매개변수 최적화)
	| > 데이터셋으로 강제로 만들었다?

분류 모델 > 뭔가 뭉쳐져 있다.

for fold, (train_idx, valid_idx) in enumerate(skf.split()): #skf.split() : 인덱스가 있는 것들이 매개변수로

정규화는 split 이후에 하는 게 좋다(책에서 찾아보자) - 전에 하는 게 좋지만 되돌릴 수 없어서

교차검증을 파이프라인에 넣어도 되지만, 그렇게 하면 결과값만 나와서 중간과정을 알 수 없고, 몇 번 fold에서 좋아졌는지 알 수 없어 + 값도 못 가져와
(책 교차검증에 있는 for문을 봐야...)

파이썬 내장함수 중에서 for (zip / enumerate) | lambda (map / filter) + list comprehensive 공부

DA의 코드는 최장 30일이 지나면 폐기 > 개선된 코드 사용
파이프라인 1 ........., SVC()
파이프라인 2 ........., RF()
파이프라인 3 ........., DT()
>> 전처리 마지막에 fit()? 파이프라인 한 방에 돌리려고 강제한 것.


